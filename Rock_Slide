Rock Slide
1.	游戏规则：
0 = Dirt 玩家走入后变成5
1 = Rock 受重力；可被玩家从侧面推入相邻空格
2 = NPC 
3 = Money 收集后变成5，prizeleft--
4 = Solid wall 不可通过
5 = Empty space 石头可落入
6 = Player start 玩家出生点：加载后变为5

2.物理
•	只有 石头 受重力，玩家不受重力
•	石头 只垂直下落 到下方空格，不做 斜向滚落
•	石头下方是玩家 → 压死（扣 1 命并重载本关）
•	石头叠在石头上则停住

3.胜负：
•	本关所有金钱收完 → 下一关
•	最后一关完成 → 结束画面
•	每关 9 条命，无计时器
•	命用完 → 回 标题画面

4.操作：
方向键：移动（每帧最多动一格），推石，挖土，收钱
Start：充值当前关卡
A/B：下一关/上一关
主循环逻辑:读键->移动/推石头/挖土->重力迭代稳定（玩家行动之后，程序会反复模拟石头下落，直到没有任何石头还能再落一格，或者达到每帧最多尝试的次数。）->死亡/过关判断->等VBlank->写tilemap（每个格子用第几号图块）和OAM（OBJ在哪，用哪块图）


5.文件整体分区：
[常量]  图块类型 T_*、地图尺寸、状态 ST_*、按键掩码、WRAM 地址
[ROM 数据]
  - 卡带头 / 入口跳转
  - 图块像素 Tiles（ROM 8 位 → 加载时扩成 VRAM 16 位）
  - 关卡表 Levels（每关 20×16 字节）
  - 关卡指针、标题字符串、HUD 文案
[代码段]
  - 启动与主循环
  - 硬件初始化 / 等待帧
  - 输入
  - 三态 UI（标题 / 游戏 / 结束）
  - 关卡加载与地图逻辑
  - 玩家行动、重力、胜负
  - 显示（逻辑→图块、HUD、精灵、拷 OAM）
[WRAM 变量]  状态、命、金钱、玩家坐标、关卡缓冲、ShadowOAM
